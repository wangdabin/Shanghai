#/bin/bash

#业务逻辑输入参数，此处很重要，需要根据实际业务情况认真设定！！！！ 
v_month=$1

echo "
insert overwrite table ODS.ODS_CDR_GUPA_M partition (month_part='$v_month') 
    SELECT 
'$v_month', 
file_type,
file_sn,
record_type,
cdr_fee_code,
visit_area_code,
visit_local_net,
org_trm_id,
self_number,
self_area_code,
self_local_net,
self_busi_district,
self_user_type,
self_service_class,
self_region_code,
net_type,
call_type,
opp_number,
opp_number_suffix,
opp_area_code,
opp_local_net,
opp_busi_district,
opp_number_type,
opp_dealer_code,
opp_service_class,
opp_region_code,
service_code,
charge_date,
charge_time,
call_duration,
self_msc_id,
self_lac_id,
self_cell_id,
opp_msc_id,
opp_lac_id,
opp_cell_id,
in_trunk,
out_trunk,
trunk_type,
base,
land,
ss,
other,
base_times,
land_times,
ss_times,
other_times,
pay_flag,
tele_service,
bearer_service,
ss_code,
imsi,
third_number,
msrn,
boundary_cell_flag,
same_city_flag,
card_no,
file_name,
reserve,
insert_date,
sys_type,
speed,
hot_billing
FROM SRC.CDR_GUPA;
"


